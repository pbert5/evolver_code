❯ 1. commit, 2. we need to add in flake tests and pytests bc its still not working, 3. you forgot about tabs, which would be a similar style to lazy git,the thought is window 2 is dedicate to live things, or were live, so the first tab would be experiments but the second tab
  would be
    evolver units( assuming we will have more then one connected to the computer( each evolver would be each arduino controlling 1 or more bioreactors)) then tab3 is proccesses( swap tabs with  [ ]). window 3 is like an inventory, ( it is technicaly seperate from window 2 in
  terms of context) the first tab will be a list of the protocols avilible ( obcv with fuzy search availible, realy every window will have like a shortcut for fuzzy search) where we can enter a protocol using space or enter which will set window 4 and 5 into the context of that
  protocl thats how we will edit it 4 will be for steps and 5 will be for components( entries scoped to the protocl, selections scoped to the step), then the next tabs in window 3 will be for materials ( this is where we will be listing like medias, bact or yeast samples, carbon
  sources ( each of these line entiers will contain id, name, meta data tag( like type, see the metadata guide in /home/ash/Documents/work/evolver_code/metadata/Bio Automation plate submission_1.0.5_ALE_culture_plate_template.xlsx as a rough refference, then the other tab will
  be devices this is where we list and static configs of the bioreactors, fluidics, spinners, and such, here we will also be able to set assocations of the things that will be static for an experiments run, i.e. what the input and output of what pumps are connected to like
  materials with certain metadata tags  can be used as input(like media, or reagents, or bact/yeast sources), while others can be used as output( like waste, or  sample or bioreactors can be either io, we will also be able to set wich actual hardware from involved evolvers is
  associated with the described hardware entries  in the protocol ( allowing reusablility on diverse systems)