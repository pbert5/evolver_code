# TUI Agent Instructions

When changing TUI behavior, layout, controls, or visual design, update
`docs/plan/tui_design.md` in the same step so the design notes stay current
with the implementation.

When a TUI task is complete and verification passes, commit the scoped changes
before handing the work back unless the user explicitly asks not to commit.
